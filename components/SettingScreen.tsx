import React from 'react';
import { Text, View } from 'react-native';
const SettingScreen = ()=>{
    return(
        <view style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{ color:'black', fontSize:20 }}>Setting Screen</Text>
        </view>
    );
}

export default SettingScreen;