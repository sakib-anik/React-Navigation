import React from 'react';
import { Text, View } from 'react-native';
const DetailsScreen = ()=>{
    return(
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{ color:'black', fontSize:20 }}>Details Screen</Text>
        </View>
    );
}

export default DetailsScreen;